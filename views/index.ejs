<!DOCTYPE html>
<html lang="fr">
<head>
  <meta charset="UTF-8">
  <title>Application Services Web</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
  <link href="https://cdnjs.cloudflare.com/ajax/libs/animate.css/4.1.1/animate.min.css" rel="stylesheet">
  <link href="/style.css" rel="stylesheet">
  <style>
    body { background: linear-gradient(120deg, #f8fafc 0%, #e0e7ef 100%); min-height: 100vh; }
    .container { max-width: 900px; margin-top: 60px; background: #fff; border-radius: 16px; box-shadow: 0 4px 24px #0001; padding: 32px; }
    h1 { font-weight: bold; color: #2c3e50; }
    .result-card { margin-top: 24px; }
    .badge { font-size: 1em; }
    .nav-tabs .nav-link { color: #2c3e50; }
    .nav-tabs .nav-link.active { font-weight: bold; }
    .tab-content { padding: 20px 0; }
    .animate__animated { animation-duration: 0.5s; }
  </style>
</head>
<body>
  <div class="container animate__animated animate__fadeIn">
    <h1 class="text-center mb-4 animate__animated animate__fadeInDown">🔎 Application Services Web</h1>
    <ul class="nav nav-tabs" id="myTab" role="tablist">
      <li class="nav-item" role="presentation">
        <button class="nav-link active" id="rest-tab" data-bs-toggle="tab" data-bs-target="#rest" type="button" role="tab">REST</button>
      </li>
      <li class="nav-item" role="presentation">
        <button class="nav-link" id="soap-tab" data-bs-toggle="tab" data-bs-target="#soap" type="button" role="tab">SOAP</button>
      </li>
      <li class="nav-item" role="presentation">
        <button class="nav-link" id="graphql-tab" data-bs-toggle="tab" data-bs-target="#graphql" type="button" role="tab">GraphQL</button>
      </li>
    </ul>
    <div class="tab-content" id="myTabContent">
      <!-- Onglet REST -->
      <div class="tab-pane fade show active" id="rest" role="tabpanel">
        <%- include('partials/forms', { mode: 'rest' }) %>
        <div id="restResult"></div>
      </div>
      <!-- Onglet SOAP -->
      <div class="tab-pane fade" id="soap" role="tabpanel">
        <%- include('partials/forms', { mode: 'soap' }) %>
        <div id="soapResult"></div>
      </div>
      <!-- Onglet GraphQL -->
      <div class="tab-pane fade" id="graphql" role="tabpanel">
        <%- include('partials/forms', { mode: 'graphql' }) %>
        <div id="graphqlResult"></div>
      </div>
    </div>
  </div>
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
  <script src="https://unpkg.com/axios/dist/axios.min.js"></script>
  <script src="/app.js"></script>
  <script src="/validation.js"></script>
  <script>
    // Les scripts pour gérer les formulaires et afficher les résultats seront inclus ici ou dans un fichier séparé
  </script>
</body>
</html> 